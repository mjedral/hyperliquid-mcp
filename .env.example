# Hyperliquid RAG and MCP Environment Configuration
# Copy this file to .env and fill in your actual values

# =============================================================================
# RAG SYSTEM CONFIGURATION
# =============================================================================

# OpenAI API Key (required for embedding generation)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# Base URL for GitBook documentation to crawl
RAG_BASE_URL=https://hyperliquid.gitbook.io/hyperliquid-docs

# Embedding provider: openai | local (default: openai)
EMBEDDING_PROVIDER=openai

# Embedding model name (default: text-embedding-3-small)
EMBEDDING_MODEL=text-embedding-3-small

# Cache directory for crawled content (default: .cache/rag)
# RAG_CACHE_DIR=.cache/rag

# SQLite database path for vector storage (default: .cache/rag/vectors.db)
# RAG_DB_PATH=.cache/rag/vectors.db

# Maximum pages to crawl (default: 1000)
# RAG_MAX_PAGES=1000

# Rate limit between requests in milliseconds (default: 1000)
# RAG_RATE_LIMIT_MS=1000

# =============================================================================
# MCP SYSTEM CONFIGURATION (Future Implementation)
# =============================================================================

# Hyperliquid network: testnet | mainnet (default: testnet)
# HL_NETWORK=testnet

# Custom HTTP API endpoint (optional)
# HL_HTTP_URL=https://api.hyperliquid-testnet.xyz

# Custom WebSocket endpoint (optional)
# HL_WS_URL=wss://api.hyperliquid-testnet.xyz/ws